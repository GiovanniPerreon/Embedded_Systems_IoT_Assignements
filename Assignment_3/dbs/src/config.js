// Dashboard config file
// Questo valore DEVE corrispondere a Config.HTTP_PORT in Java
window.CONFIG_HTTP_PORT = 8080;
// Base URL per le API del backend
window.CONFIG_API_BASE = `http://localhost:${window.CONFIG_HTTP_PORT}/api`;
// Intervallo di polling in ms
window.CONFIG_DOM_UPDATE_RATE = 100;
